#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, i;
    cin >> n;
    for (i = 0; i < n; i++) {
        cout << setw(13 * i) << setiosflags(ios::left) << " " << "hello world!" << endl;
    }
    return 0;
}
